$(document).ready(function() {
    
    // Button click event to trigger the toggle
    $("#toggle-btn").click(function() {
        
        // Switching the visibility of the paragraph
        $(".info-text").toggle();
        
    });

});
